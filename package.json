{
  "name": "product-ingestion-service",
  "version": "1.0.0",
  "description": "A service to ingest product data",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --external:better-sqlite3 && mkdir -p dist/database && cp database/local.db dist/database/",
    "test": "NODE_ENV=test jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/better-sqlite3": "^7.6.4",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.8",
    "drizzle-kit": "^0.19.13",
    "esbuild": "^0.25.10",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "aws-sdk": "^2.1437.0",
    "better-sqlite3": "^9.4.5",
    "drizzle-orm": "^0.28.2"
  }
}
